import '@testing-library/jest-dom'

// jsdom não implementa window.matchMedia — necessário para Welcome.jsx (detecção de PWA standalone)
Object.defineProperty(window, 'matchMedia', {
  writable: true,
  value: (query) => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: () => {},
    removeListener: () => {},
    addEventListener: () => {},
    removeEventListener: () => {},
    dispatchEvent: () => false,
  }),
})
